#vault
This folder contains curates study content.
##Add a module
Copy the template folder:

- vault/modules/_template - > vault/modules/<MODULE_CODE>

Example:

- vault/modules/_template - > vault/mmodules/CSC2059

Keep notes in Markdown if possible.
